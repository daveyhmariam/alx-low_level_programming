#include "main.h"
#include <stdio.h>
/**
 * is_paindrom: palindrom checker with recursion
 * @s:a poiter to string
 * Return:1 if a string is a palindrome and 0 if not.
*/
int is_palindrome(char *s)
{
    
}