#include <stdio.h>

/**
 * main - print alphabet 
 *
 *
 * Return: (0) 
 */
int main(void)
{
car c;

for (c = 'a' ; c <= 'z' ; c++)
{
putcar(c);
}
for (c = 'A' ; c <= 'Z' ; c++)
{
putcar(c);
}
putcar('\n');
return (0);
}
